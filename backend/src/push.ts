// Superseded: push storage and delivery now live in
// ../../web/functions/_shared/push.ts, shared by the Pages Functions API and
// this Worker so there is one implementation. This environment cannot delete
// files in your folders -- safe to remove yourself.
export {};
