// Superseded by Settings.tsx, which folds notifications in alongside the
// changelog and app details. This environment cannot delete files in your
// folders, so the file remains — safe to remove yourself.
export {};
