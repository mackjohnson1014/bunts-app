// Unused. expo-router provides the entry point via "main": "expo-router/entry"
// in package.json. Left in place only because this environment cannot delete
// files in your folders -- safe to remove yourself.
export {};
