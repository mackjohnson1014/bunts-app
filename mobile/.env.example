# Leave unset to run the app on the fixtures in src/api/mock.ts.
# Set both once the backend is deployed.
# EXPO_PUBLIC_BUNTS_API=https://bunts-backend.<your-subdomain>.workers.dev
# EXPO_PUBLIC_BUNTS_SECRET=<the APP_SECRET you set with wrangler secret put>
